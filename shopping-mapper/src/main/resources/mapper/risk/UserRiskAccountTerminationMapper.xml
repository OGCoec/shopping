<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.ShoppingSystem.mapper.risk.UserRiskAccountTerminationMapper">

    <insert id="upsertRiskTermination">
        <![CDATA[
INSERT INTO user_risk_account_termination (
    id,
    user_id,
    email,
    email_hash,
    phone,
    phone_hash,
    termination_reason,
    terminated_at,
    created_at
) VALUES (
    #{id},
    #{userId},
    #{email},
    #{emailHash},
    #{phone},
    #{phoneHash},
    #{terminationReason},
    #{terminatedAt},
    #{createdAt}
)
ON CONFLICT (email_hash) DO UPDATE
SET user_id = EXCLUDED.user_id,
    email = EXCLUDED.email,
    phone = EXCLUDED.phone,
    phone_hash = EXCLUDED.phone_hash,
    termination_reason = EXCLUDED.termination_reason,
    terminated_at = EXCLUDED.terminated_at
        ]]>
    </insert>

    <select id="countTerminatedEmailHashes" resultType="long">
        <![CDATA[
SELECT COUNT(1)
FROM user_risk_account_termination
WHERE email_hash IS NOT NULL
  AND btrim(email_hash) <> ''
        ]]>
    </select>

    <select id="listTerminatedEmailHashes" resultType="string">
        <![CDATA[
SELECT email_hash
FROM user_risk_account_termination
WHERE email_hash IS NOT NULL
  AND btrim(email_hash) <> ''
ORDER BY email_hash
LIMIT #{limit}
OFFSET #{offset}
        ]]>
    </select>

    <select id="existsByEmailHash" resultType="boolean">
        <![CDATA[
SELECT EXISTS (
    SELECT 1
    FROM user_risk_account_termination
    WHERE email_hash = #{emailHash}
    LIMIT 1
)
        ]]>
    </select>

    <select id="deleteExpiredRiskTerminatedIdentities" resultType="int">
        <![CDATA[
WITH target AS (
    SELECT uli.user_id
    FROM user_login_identity uli
    JOIN user_risk_account_termination termination
      ON termination.user_id = uli.user_id
    WHERE uli.status = 'RISK_TERMINATED'
      AND termination.terminated_at <= #{cutoff}
    ORDER BY termination.terminated_at, uli.user_id
    LIMIT #{limit}
    FOR UPDATE OF uli SKIP LOCKED
),
deleted AS (
    DELETE FROM user_login_identity uli
    USING target
    WHERE uli.user_id = target.user_id
    RETURNING uli.user_id
)
SELECT COUNT(1)
FROM deleted
        ]]>
    </select>
</mapper>
